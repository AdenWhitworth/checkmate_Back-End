/**
 * Represents the arguments required when a player reconnects to a bot game.
 * 
 * @interface ReconnectBotGameArgs
 * 
 * @property {string} gameId - The game ID associated with the bot game being reconnected to. 
 */
export interface ReconnectBotGameArgs {
    gameId: string;
}