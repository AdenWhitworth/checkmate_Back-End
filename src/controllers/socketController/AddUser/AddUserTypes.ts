/**
 * Represents the arguments required to add a user.
 * 
 * @interface AddUserArgs
 * 
 * @property {string} username - The username of the user to be added. Must be a non-empty string.
 */
export interface AddUserArgs {
    username: string;
};

export interface AddUserArgs {
    username: string
};